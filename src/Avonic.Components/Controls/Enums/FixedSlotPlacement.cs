namespace Avonic.Components.Controls;

public enum FixedSlotPlacement
{
    /// <summary>Fixed content is rendered above the scrollable content in z-order.</summary>
    Before,
    /// <summary>Fixed content is rendered above the scrollable content in z-order (default).</summary>
    After,
}
