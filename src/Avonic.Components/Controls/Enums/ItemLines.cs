namespace Avonic.Components.Controls;

public enum ItemLines
{
    /// <summary>Full-width bottom border (default).</summary>
    Full,
    /// <summary>Bottom border inset from the left edge, starting after the start slot.</summary>
    Inset,
    /// <summary>No bottom border.</summary>
    None,
}
