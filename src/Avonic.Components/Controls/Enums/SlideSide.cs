namespace Avonic.Components.Controls.Enums;

/// <summary>Side on which <see cref="AvonicItemOptions"/> are revealed.</summary>
public enum SlideSide
{
    /// <summary>Options are revealed by swiping right (start side).</summary>
    Start,

    /// <summary>Options are revealed by swiping left (end side).</summary>
    End
}
